### SSH Brute-Force Auditor

A streamlined Python script for auditing SSH security by testing a single username against a password wordlist. It uses the **Paramiko** library for SSH connectivity and features a `tqdm` progress bar for real-time status updates.

## Installation
```bash
pip install paramiko
pip install tqdm
